# Graph Reader Prompt

Use `data/graph.json` as the first source of context. Identify the target node,
its source file, community, direct neighbors, and reverse-impact edges before
opening source files.

