# Fix Planner Prompt

Convert the root cause into a small suggested patch plan. Preserve public
behavior unless the failing test proves it is the defect. Include one regression
test that would fail on the current broken implementation. Do not modify files.
