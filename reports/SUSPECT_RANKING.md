# Suspect Ranking Report

This report ranks graph nodes as bug **suspects** for a graph-guided debugging workflow. It is generated by `agent/rank_suspects.py` from `data/graph.json` (assignment extension, §5.6).

## Method

Each node is scored by combining two signals computed over the **undirected** version of the graph:

- **Degree centrality** = `degree / (n_nodes - 1)` — how connected the node is (wide blast radius if buggy).
- **Proximity to the seed** = `1 / (1 + distance_to_seed)`, where `distance_to_seed` is the BFS shortest-path distance (in hops) from the seed node. Unreachable nodes have distance `inf` and proximity `0`.

The combined **suspect score** is:

```
score = degree_centrality + 1 / (1 + distance_to_seed)
```

This rewards nodes that are both well-connected and close to the seed, so the seed itself and its well-connected neighbours rank highest — telling the agent which nodes to inspect first.

**Seed node:** `foo()`

## Top 3 suspects

| Rank | Node | Degree | Degree centrality | Distance to seed | Suspect score |
| ---: | --- | ---: | ---: | ---: | ---: |
| 1 | `foo()` | 3 | 0.167 | 0 | 1.167 |
| 2 | `__init__.py` | 9 | 0.500 | 1 | 1.000 |
| 3 | `io.py` | 7 | 0.389 | 2 | 0.722 |
